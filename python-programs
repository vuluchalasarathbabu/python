=============================================
1.How to save file with file name from user using Python?

# import pathlib module
import pathlib

# path of this script
directory = "D:\gfg\\"

# get fileName from user
filepath = directory + input("Enter filename:")

# To create a file
pathlib.Path(filepath).touch()

=========================================================
2. rename source file name with target file name

# import pathlib module
import pathlib

# get source file name
src = input("Enter src filename:")

# get destination file name
target = input("Enter target filename:")

# rename source file name with target file name
pathlib.Path(src).rename(target)


=================================================================
3.copies source file to a new destination file

# import shutil module
import shutil

# get source file name
src = input("Enter src filename:")

# get destination file name
dest = input("Enter target filename:")

# copies source file to a new destination file
shutil.copyfile(src, dest)

==============================================================



2. covid vaccination program for b/w take the input form 18 to 45 age otherwise won't allow for the vaccination

def covid(num):
    if num >= 18 and num <= 45:
        return "you are allowd forvaccination"
    else:
        return "you are not allowd for vaccination"
#x=input(int("entet the num:")

number = int(input("Enter second number: "))

print(covid(number))

============================================================
3. Current date and time

import sys
import datetime
now = datetime.datetime.now()
print ("python version")
print (sys.version)
print ("current date and time: ")
print (now.strftime("%m/%y/%d"))

================================================================







































































































































































































































































